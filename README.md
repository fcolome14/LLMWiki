# LLMWiki
LLM Wiki implementation with Obsidian

## How to run it?

```bash
uv run main.py "which notes don't link to anything yet?"
```

```bash
uv run main.py "summarize what I know about agent orchestration" --vault path/to/vault
```